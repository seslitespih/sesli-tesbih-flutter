name: sesli_tesbih
description: Sesli Tesbih - Sesli İslami Zikir Sayacı
publish_to: 'none'
version: 1.3.0+4

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  speech_to_text: ^6.6.2
  adhan_dart: ^2.0.1
  geolocator: ^12.0.0
  geocoding: ^3.0.0
  shared_preferences: ^2.3.3
  permission_handler: ^11.3.1
  flutter_compass: ^0.8.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
